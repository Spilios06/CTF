import fileinput

def lines():
    for i in fileinput.input():
        obj = []
        obj.append(line)
        return obj

def mklist():
    for j in range(length):
        column = []
        for i in range(length):
            column.append(lines(""))
        obj.append(column)


def outp():
    answere = 1
    print(answere)

lines()
